import { SplitYoutubePipe } from './split-youtube.pipe';

describe('SplitYoutubePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitYoutubePipe();
    expect(pipe).toBeTruthy();
  });
});
